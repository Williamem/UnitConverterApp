import { unitDefinitions } from '../../constants/unitDefinitions';

/* export const convertToBase = (category: string, value: number, fromUnit: string): number => {
  const conversionFactor = unitDefinitions[category].conversions[fromUnit];
  return value * conversionFactor;
};

export const convertFromBase = (category: string, value: number, toUnit: string): number => {
  const conversionFactor = unitDefinitions[category].conversions[toUnit];
  return value / conversionFactor;
}; */

